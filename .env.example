# Copy this file to `.env` on deployment hosts and fill real values there.
# Do not commit real secrets.

CATMAPPER_AUTH_SECRET=replace-with-hex-secret
VERSION=dev
CATMAPPER_AUTH_TOKEN_TTL_SECONDS=2592000
CATMAPPER_UPLOAD_QUEUE=upload
CATMAPPER_WAITING_USES_QUEUE=waitingUSES
